/**
 * Renders the index route of the "/list" route.
 * By default renders all the lists, the user is
 * a member of.
 */
export default function ListIndexRoute() {
  return (
    <>
      <p>Select one of the lists.</p>
    </>
  );
}
